User has asked to reset their password via email {{ email }}. Follow the link below:

{{protocol}}:://{{domain}} {% url "password_reset_confirm" uidb64=uid token=token %}

Here's your username:
{{user.get_username}}